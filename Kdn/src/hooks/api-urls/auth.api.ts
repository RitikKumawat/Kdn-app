import {CONSTANTS} from '../../constants/index.constants';

export const authApis = {
  getProfile: `${CONSTANTS.API_PREFIX}/get-profile`,
  login: '/auth/admin-login',
  logout: `${CONSTANTS.API_PREFIX}/logout`,
};
